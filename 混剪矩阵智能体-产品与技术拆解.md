# 混剪矩阵智能体：产品与技术拆解

> 分析对象：本机已安装并运行的 `videomix v1.0.5`  
> 分析方式：界面与可访问性结构、安装目录、运行进程、监听端口、应用日志、Electron 前端包及本地数据结构的只读分析。  
> 说明：下文把“已验证事实”和“基于证据的推断”分开标注。未进行源码破解、接口密钥读取或账号操作。

## 一句话结论

这不是一个以 AI 视频理解为核心的产品，而是一套“素材分类管理 + 模板化分镜 + AI 文案/TTS + FFmpeg 本地合成 + 浏览器自动发布”的桌面工作流系统。

真正形成效率壁垒的不是某个大模型，而是：

1. 把混剪过程抽象成结构化镜头槽位；
2. 让镜头槽位和素材目录建立稳定映射；
3. 自动完成时长对齐、裁切、字幕、音乐、配音和编码；
4. 用任务中心承接批量生产；
5. 用独立浏览器用户目录保存各平台账号登录态，再由 Playwright 自动发布。

## 1. 已验证的技术架构

### 1.1 桌面端

- Electron `41.1.1`
- Chromium `146.0.7680.166`
- Node.js `24.14.0`
- 应用版本 `1.0.5`
- 前端构建产物由 Vite 打包
- 前端框架：Vue `3.5.30`
- UI 组件：Element Plus `2.13.6`
- 状态管理：Pinia `3.0.4`
- 路由：Vue Router `5.0.4`，使用 Hash Router
- HTTP 客户端：Axios `1.14.0`

安装包中的 `package.json` 明确列出了 Vue、Vue Router、Pinia、Axios、Element Plus。界面资源按页面进行异步拆包，例如：

- `WorkbenchPage`
- `AccountPage`
- `AssetsCenterPage`
- `VideoMix`
- `BatchGenerationPage`
- `PublishAccountsPage`
- `ReleaseSchedulePage`
- `SystemSettingsPage`

### 1.2 Electron 主进程

Electron 主进程承担的并不只是窗口管理，还提供了一个本地能力网关。Renderer 不直接访问任意文件系统，而是通过 preload 暴露的 `electronAPI` 调用 IPC。

已验证的 IPC 能力包括：

- 文件/文件夹选择
- 盘符和目录读取
- UTF-8 文件读写
- 二进制文件写入
- 复制文件、创建目录、清空目录
- 扫描素材根目录
- 创建素材分类文件夹
- 导入软件内置素材
- 读取字体、背景音乐目录
- 管理自定义混剪模板
- 调用百炼兼容接口
- 同步发布账号和发布排期
- 打开本地路径
- 查询本地后端状态

这层设计值得复刻：Renderer 只做产品界面，系统级能力收敛到 Electron 主进程，Python 后端只承担重计算和自动化。

### 1.3 本地后端

- 后端进程：`KrLongAI.exe`
- 打包方式：Python 3.11 + PyInstaller
- Web 框架：Flask `3.0.3`
- 监听地址：`0.0.0.0:35006`
- 桌面端实际通过 `http://127.0.0.1:35006` 调用
- 后端应用模块名：`app6.app.main`

已验证的主要 Python 模块：

- `app6.app.clip_v3`
- `app6.app.publish`
- `app6.app.text`
- `app6.app.activation`
- `app6.app.cpu_adaptive`
- `utils.ffmpeg.*`
- `utils.uploader.douyin`
- `utils.uploader.kuaishou`
- `utils.uploader.shipinhao`
- `utils.uploader.xiaohongshu`

后端随包附带：

- `ffmpeg.exe`
- `ffprobe.exe`
- Chromium 128
- Playwright
- NumPy
- Pillow
- FontTools
- aiohttp
- PySide6 / QFluentWidgets Pro

### 1.4 已识别的本地接口

视频合成：

- `POST /video/clip_v3/`
- `GET /video/clip_v3/progress/`
- `POST /video/clip_v3/cancel/`
- `GET /video/clip_v3/gpu_encoder/`

发布：

- `POST /publish/`
- `POST /publish/account/`
- `POST /publish/account/status/`

文件：

- `/files/`
- `/files/upload/`

前端采用轮询进度，而不是 WebSocket。日志显示创建任务后约每 5～6 秒请求一次 `/video/clip_v3/progress/`。

## 2. 产品模块拆解

### 2.1 工作台

产品定位应是全局经营仪表盘，而不是生产编辑器。通常展示：

- 素材数量和素材健康度
- 待执行/执行中/已完成任务
- 待发布和已发布数量
- 快速入口
- 最近任务与异常

复刻时工作台可以放在二期；它不是核心闭环的阻塞模块。

### 2.2 账号档案

这里的“账号”更像内容人格/品牌档案，而不是发布平台账号。它给文案生成提供稳定上下文：

- 品牌/门店/人物信息
- 行业与产品
- 人设
- 语言风格
- 禁用词
- 事实素材
- CTA

技术上应保存为结构化 Persona Profile，并在百炼 Prompt 中拼接，而不是简单放一个大文本框。

### 2.3 素材中心

已验证的软件素材目录按文件夹分类，当前存在：

- 产品
- 人物
- 店内环境
- 服务过程
- 操作过程
- 空镜
- 门头

素材中心扫描本地目录，统计支持的视频文件，并标记“素材较少”分类。它不是 DAM 资产库，而是“本地文件夹索引器”。

建议的数据结构：

```ts
type AssetCategory = {
  id: string
  name: string
  folderPath: string
  assetCount: number
  lastScannedAt: number
  health: "ok" | "insufficient" | "empty"
}

type VideoAsset = {
  id: string
  categoryId: string
  path: string
  duration: number
  width: number
  height: number
  fps: number
  codec: string
  fileSize: number
  fingerprint?: string
}
```

原产品大概率只做目录扫描和数量统计；复刻版建议额外建立 SQLite 索引，避免每次全量 `ffprobe`。

### 2.4 镜头剪辑

这是产品真正的“模板配置器”。已验证的自定义模板把成片拆成一组 `folderGroups`，每一项代表一个镜头槽位：

```json
{
  "folder": "产品",
  "muteOriginal": true,
  "durationSec": 0,
  "script": "",
  "scriptOriginal": "本镜头口播文案",
  "sentenceLabel": "镜头1",
  "sentenceRole": "自定义",
  "assetCategory": "产品"
}
```

因此它的模板本质是：

```text
模板
 ├─ 镜头 1：人物类素材 + 口播句 1
 ├─ 镜头 2：产品类素材 + 口播句 2
 ├─ 镜头 3：操作过程素材 + 口播句 3
 └─ 镜头 N：门头素材 + CTA
```

镜头不是在传统 NLE 时间线上逐帧编辑，而是以“语义镜头槽位”配置。用户只需要指定：

- 本镜头用哪类素材
- 本镜头说什么
- 原声是否静音
- 指定时长或自动时长
- 字幕/标题样式

### 2.5 任务中心

任务中心是批量生成器。任务快照中包含：

- 模板类型与模板名
- 所有镜头
- 每镜头候选视频源
- 文案原文和改写文案
- 画布参数
- 配音开关、音量、语速
- MiniMax API Key 和音色 ID 的任务快照
- 字幕配置
- 背景音乐配置
- 编码器和码率
- 工作目录与输出目录

“任务快照”非常重要：一旦任务开始，后续修改全局设置不能影响正在生成或等待生成的任务。

### 2.6 发布账号管理

已验证的平台至少包括：

- 抖音
- 小红书

后端模块还包含：

- 快手
- 视频号

每个发布账号的关键字段：

```ts
type PublishAccount = {
  id: string
  name: string
  platform: "douyin" | "xiaohongshu" | "kuaishou" | "shipinhao"
  userDataDir: string
  linkStatus: "unknown" | "linked" | "expired" | "failed"
  publishStatus: string
  isDefault?: boolean
}
```

`userDataDir` 是关键：每个账号对应一个独立 Chromium 用户数据目录，用于持久化 Cookie 和登录态。

### 2.7 发布排期

本地排期数据保存为 JSON，任务下可配置多个发布目标。每个目标都有独立状态：

```ts
type PublishTarget = {
  accountId: string
  userDataDir: string
  phase: "pending" | "scheduled" | "publishing" | "published" | "failed"
  scheduledAt?: number
  publishMessage?: string
  lastAttemptAt?: number
  nextRetryAt?: number
}
```

Electron 主进程每 15 秒扫描一次排期，找到已到期任务后调用：

```json
POST /publish/
{
  "platform": "douyin",
  "video_file": "D:/.../output.mp4",
  "title": "标题",
  "topics": ["话题1", "话题2"],
  "auto_publish": true,
  "cover": "D:/.../cover.jpg",
  "user_data_dir": "D:/.../browser-profile/account-1"
}
```

这说明排期器在 Electron 主进程，实际发布执行器在 Python 后端。

## 3. 混剪合成的底层逻辑

### 3.1 推测的完整流水线

```text
用户选择模板
  → 读取 Persona/原始文案
  → 百炼改写并按镜头数切句
  → 每句绑定一个素材分类
  → 从分类目录选择候选素材
  → MiniMax 对每句生成音频
  → 以音频时长决定镜头时长
  → 视频裁切/循环/缩放到竖屏画布
  → 生成字幕时间轴
  → 合成标题、字幕、配音和 BGM
  → GPU/CPU 编码输出 MP4
  → 写入任务中心和发布排期
```

### 3.2 文案生成

已验证接口：

- `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`

这表明百炼以 OpenAI 兼容的 Chat Completions 方式接入。前端存在：

- `bailianApiKey`
- `bailianRewriteInstruction`
- `persona`
- `personaProfiles`

并存在抖音分享文本提取后的改写流程。Prompt 规则强调：

- 原文压缩成适合镜头的短口播
- 每条 copywriting 对应一个镜头
- 人设事实优先
- 不增加原始文案和人设中不存在的事实

复刻时必须要求模型返回结构化 JSON，不能依赖自然语言分隔：

```json
{
  "shots": [
    {
      "role": "hook",
      "assetCategory": "人物",
      "copywriting": "一句口播",
      "durationHint": 2.8
    }
  ]
}
```

### 3.3 配音

已验证 MiniMax 接口：

- `GET /v1/get_voice`
- `POST /v1/files/upload`
- `POST /v1/voice_clone`
- `POST /v1/t2a_v2`

TTS 请求包含：

- `model`
- `text`
- `stream: false`
- `voice_setting.voice_id`
- `speed`
- `vol`
- `pitch`
- `emotion`
- `subtitle_enable`
- `language_boost`

任务会对 MiniMax 限流进行等待和重试。配音按镜头逐条生成，而不是整篇一次生成，这样更容易：

- 精确对齐镜头
- 单镜头失败重试
- 替换单句文案
- 控制字幕时间轴

### 3.4 素材选择

根据模板和任务数据，可判断素材选择策略主要是规则驱动：

1. 每个镜头指定一个素材分类；
2. 扫描该分类目录得到候选视频；
3. 过滤不支持或时长不足的视频；
4. 随机、轮换或按近期使用历史选择；
5. 选择起始裁切点；
6. 原视频不足时允许循环，日志中存在 `video_source_too_short.loop`；
7. 多次批量生成时尽量避免同一位置重复使用同一片段。

建议复刻版实现“去重窗口”：

```text
同一模板最近 20 个成片中：
- 同一镜头位不重复相同素材；
- 相邻镜头不取同一个源文件；
- 相同素材尽量使用不同时间区间；
- 用 perceptual hash 检测高度相似片段。
```

### 3.5 FFmpeg 时间线

后端包内已验证存在以下模块：

- `utils.ffmpeg.clip.audio`
- `utils.ffmpeg.clip.base`
- `utils.ffmpeg.clip.draw_box`
- `utils.ffmpeg.clip.draw_text`
- `utils.ffmpeg.clip.duration`
- `utils.ffmpeg.clip.image`
- `utils.ffmpeg.clip.subtitles`
- `utils.ffmpeg.clip.trim`
- `utils.ffmpeg.clip.video`
- `utils.ffmpeg.context`
- `utils.ffmpeg.exporter`
- `utils.ffmpeg.project`
- `utils.ffmpeg.time_line`
- `utils.ffmpeg.track`

因此它不是简单执行一个 `concat` 命令，而是在 Python 中建立了类似轻量 NLE 的对象模型：

```text
Project
 └─ Timeline
     ├─ Video Track
     │   ├─ VideoClip
     │   ├─ ImageClip
     │   └─ DrawBox / DrawText
     ├─ Subtitle Track
     │   └─ SubtitleClip
     └─ Audio Track
         ├─ Voice AudioClip
         └─ BGM AudioClip
```

最终由 Exporter 生成 FFmpeg filter graph 和编码命令。

### 3.6 时长对齐

最合理且与前端数据吻合的策略是：

- 有配音：镜头时长以该镜头 TTS 音频时长为主；
- 无配音：使用模板 `durationSec`；
- `durationSec = 0`：自动时长；
- 源视频足够长：随机选择起点后裁切；
- 源视频不足：循环或降级换素材；
- 视频过短且不适合循环：跳过，前端代码存在 `skip_too_short`。

### 3.7 字幕与标题

前端任务包含：

- `enableSubtitle`
- `font_file`
- `font_size`
- `margin_v`
- `subtitle_effect`
- 标题设置

字体目录中内置多款中文字体。字幕可能采用 SRT/ASS 后由 FFmpeg subtitles filter 渲染，也可能由内部 SubtitleClip 生成 filter graph。前端明确存在 SRT 设置，因此优先判断为“生成 SRT/ASS，再烧录到视频”。

### 3.8 背景音乐

背景音乐配置包含：

- 开关
- 音乐目录
- 音量
- 指定曲目

安装包内置两首 FLAC。合成时应：

- 对 BGM 进行裁切或循环；
- 调低 BGM 音量；
- 与逐镜头 TTS 拼接后的音轨混合；
- 首尾做淡入淡出；
- 无配音时可以保留部分素材原声；
- 有配音时通常静音原视频。

### 3.9 编码与性能

启动后前端首先请求 `/video/clip_v3/gpu_encoder/`。这表示后端会检测当前设备可用的硬件编码器，并选择 GPU 或 CPU 路径。

建议检测顺序：

```text
NVIDIA → h264_nvenc
Intel → h264_qsv
AMD → h264_amf
无可用 GPU → libx264
```

“快速拼接”的主要来源是：

- 不做内容识别；
- 不做复杂转场；
- 所有镜头结构已模板化；
- FFmpeg 单次生成 filter graph；
- 使用 GPU H.264 编码；
- 可并行准备 TTS 和素材元数据。

## 4. 自动发布的实现手法

后端打包了 Chromium 128、Playwright 和多个 `utils.uploader.*` 模块，因此自动发布不是调用官方开放平台 API，而是浏览器自动化。

典型流程：

```text
为账号创建独立 userDataDir
  → 启动持久化 Chromium Context
  → 用户首次手动扫码登录
  → Cookie/LocalStorage 保存在目录
  → 发布时打开创作者中心
  → 上传本地视频
  → 填标题和话题
  → 可选上传封面
  → 等待平台处理
  → 点击发布
  → 解析成功/失败提示
```

产品风险：

- 平台 DOM 改版会导致选择器失效；
- 验证码和风控不可稳定自动化；
- 多账号并发容易触发平台安全策略；
- 用户目录损坏会造成登录态丢失；
- 平台条款可能限制此类自动化；
- 必须做上传超时、页面截图和失败重试；
- “登录正常”与“有发布权限”应分开检测。

## 5. 本地数据与状态管理

目前产品采用“LocalStorage + JSON 文件”混合持久化：

- 自定义模板：`mix-custom-templates.json`
- 发布账号：`publish-accounts.json`
- 排期元数据：`release-schedule-meta.json`
- 页面配置和任务状态：Electron Local Storage / Pinia
- 日志：`logs/main.log`

优点是开发快，缺点是：

- 数据量增大后检索困难；
- JSON 写入中断可能损坏；
- 多进程并发一致性弱；
- 任务状态恢复复杂；
- 字符编码问题明显，当前 JSON/日志中已出现中文乱码。

复刻时建议从第一版就用 SQLite：

```text
asset_categories
assets
personas
templates
template_shots
generation_tasks
task_shots
publish_accounts
publish_jobs
app_settings
```

API Key 使用系统凭据库或加密存储，不要进入任务 JSON、LocalStorage 或日志。

## 6. 前端框架复刻建议

### 6.1 推荐保持同类架构

```text
Electron
├─ Main Process
│  ├─ Window / Tray / Auto Update
│  ├─ IPC
│  ├─ File System Gateway
│  ├─ SQLite
│  ├─ Backend Process Manager
│  └─ Scheduler
├─ Preload
│  └─ typed electronAPI
└─ Renderer
   ├─ Vue 3
   ├─ TypeScript
   ├─ Pinia
   ├─ Vue Router
   └─ Element Plus
```

建议加入：

- TypeScript
- Zod：校验 IPC 和后端返回值
- better-sqlite3：本地持久化
- VueUse：常用桌面交互
- TanStack Query 或自建任务查询层：管理轮询与缓存

### 6.2 前端页面信息架构

```text
工作台
账号档案
素材中心
镜头模板
任务中心
发布账号
发布排期
系统设置
```

“镜头剪辑”建议改名为“镜头模板”或“模板编排”，避免用户误以为它是专业剪辑时间线。

### 6.3 推荐的核心组件

- `AppSidebar`
- `PageHeader`
- `MetricCard`
- `AssetCategoryCard`
- `AssetPicker`
- `TemplateShotList`
- `ShotEditor`
- `CopywritingPanel`
- `VoiceSelector`
- `SubtitleStyleEditor`
- `GenerationTaskTable`
- `TaskProgressDrawer`
- `PublishAccountCard`
- `ScheduleCalendar`
- `VideoPhonePreview`

## 7. 后端复刻建议

### 7.1 服务边界

建议把后端拆成 5 个模块：

```text
media_probe       素材元数据、缩略图、指纹
copywriting       百炼 Prompt 与结构化输出
voice             MiniMax TTS、音色和克隆
composer          时间线、字幕、音频和 FFmpeg
publisher         Playwright 多平台发布器
```

### 7.2 任务状态机

不要只保存“已完成/失败”，建议使用：

```text
draft
queued
preparing_copy
generating_voice
selecting_assets
composing
encoding
completed
failed
canceled
```

每个任务记录：

- 总进度
- 当前阶段
- 当前镜头
- 已运行时长
- FFmpeg stderr 摘要
- 可重试错误
- 产物路径

### 7.3 任务执行模型

MVP 可以使用单进程队列：

- 一个合成 Worker；
- TTS 允许 2～3 个并发；
- FFmpeg 同时只跑 1 个或按 GPU 显存限制；
- 发布任务独立队列；
- 所有任务可取消。

成熟版再考虑：

- Redis/RQ/Celery；
- Worker 隔离；
- 断点恢复；
- 多机分发。

## 8. 建议的 MVP 范围

### 第一阶段：跑通本地成片

- 素材目录扫描
- 7 个固定素材分类
- 1 个固定 9:16 模板
- 手工输入每镜头文案
- MiniMax TTS
- FFmpeg 裁切、拼接、字幕和 BGM
- 本地任务中心

验收标准：30～60 秒竖屏视频可以稳定批量生成 10 条，任务失败能定位到具体镜头。

### 第二阶段：模板与 AI 文案

- Persona 档案
- 百炼结构化改写
- 自定义镜头模板
- 多种标题/字幕样式
- 素材去重策略
- GPU 编码检测

### 第三阶段：矩阵发布

- 独立浏览器账号目录
- 抖音单账号发布
- 小红书单账号发布
- 发布排期
- 失败截图和日志
- 登录状态检测

### 第四阶段：商业化和规模化

- 激活码/授权
- 自动更新
- 模板市场
- 云端配置同步
- 账号权限与团队协作
- 数据看板

## 9. 复刻时最容易踩的坑

1. **把 AI 当核心。** 真正难的是稳定的媒体流水线和任务状态恢复。
2. **整段生成 TTS。** 会让镜头对齐和单句重试变得困难。
3. **只按文件名随机取素材。** 很快会产生肉眼可见的重复。
4. **用前端保存任务状态。** 应由后端任务数据库作为唯一事实源。
5. **FFmpeg 命令散落在业务代码。** 应先建立 Timeline/Track/Clip 抽象。
6. **把 API Key 存 LocalStorage。** 桌面端很容易被读取。
7. **发布模块没有截图和 DOM 版本策略。** 平台改版后会变成黑盒失败。
8. **后端监听 `0.0.0.0`。** 原产品本地 Flask 服务暴露到局域网，不必要且有安全风险；复刻版应只监听 `127.0.0.1`，并增加随机令牌。
9. **使用 Flask 开发服务器交付生产。** 原产品日志已明确提示这一点；复刻版可用 Waitress/Uvicorn，或直接使用受控 IPC。
10. **忽略编码。** 当前产品数据中已出现明显 UTF-8/GBK 乱码，应统一 UTF-8，并对旧数据做迁移。

## 10. 产品经理视角的优先级判断

### 用户真正购买的结果

不是“AI 能剪视频”，而是：

- 一套素材可以快速产出很多条视频；
- 每条视频的内容结构基本正确；
- 不需要学习剪辑软件；
- 可保持账号日更；
- 能直接进入发布排期。

### 核心北极星指标

建议使用：

```text
每 100 个生成任务中的“无需人工返工即可发布”的成片数
```

辅助指标：

- 单条平均生成时长
- 单条人工操作时长
- 任务成功率
- 素材重复率
- TTS 失败率
- 发布成功率
- 单素材库可产出的合格视频数量

### 最重要的护城河

1. 行业模板质量；
2. 素材和镜头的匹配规则；
3. 批量生成的稳定性；
4. 失败可恢复；
5. 平台发布适配速度；
6. 可复用的行业 Persona 与 Prompt。

前端视觉和模型品牌都不是主要护城河。

## 11. 最终技术选型建议

如果目标是尽快复刻出 Windows 商用版：

```text
桌面端：Electron + Vue 3 + TypeScript + Element Plus
本地库：SQLite / better-sqlite3
后端：Python 3.11 + FastAPI + Pydantic
媒体：FFmpeg / ffprobe
AI 文案：百炼 OpenAI-compatible API
TTS：MiniMax t2a_v2
发布：Playwright + 独立 persistent context
打包：electron-builder + PyInstaller
日志：Electron 主日志 + Python rolling log + task event log
```

如果团队更擅长 TypeScript，也可以把素材扫描、任务调度和部分 FFmpeg 编排放进 Electron/Node，只保留 Python 发布器。但考虑到现有 Python Playwright 和媒体生态，双进程架构仍然更稳妥。

