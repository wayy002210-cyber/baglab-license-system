# 袋研官矩阵混剪工作台 0.4.1 故障修复验收记录

验收日期：2026-07-30

## 修复内容

- 百炼 `deepseek-v3` 生成选题时启用 JSON Mode，降低模型输出被 Markdown 包裹或结构不稳定导致的随机失败。
- 使用当前人设、参考脚本、当前百炼密钥和 `deepseek-v3` 真实调用选题服务，成功返回 5 个结构化选题。
- MiniMax 试听遇到账户余额不足时，返回“MiniMax 账户余额不足，请充值后再生成或试听”，不再显示 `Internal Server Error` 或 JSON 解析错误。
- Electron 本地 HTTP 桥接增加非 JSON 错误防护，后端异常也会转换为可读中文提示。
- 音色库调整为“全部用户克隆音色 + 最多 10 个中文基础音色”，用户克隆音色优先展示。

## 自动验证

- TypeScript / Vue 类型检查通过。
- Renderer / Electron：37 个测试文件、90 项测试通过。
- Python 后端：75 项测试通过。
- PyInstaller 后端健康检查通过。
- 打包后的桌面程序启动并显示 Renderer。

## 外部服务诊断结论

- 百炼密钥与 `deepseek-v3` 当前可正常生成结构化选题。
- MiniMax 密钥可正常读取音色库。
- MiniMax TTS 返回供应商错误码 `1008 insufficient balance`。需要在 MiniMax 控制台充值后才能执行收费的试听、克隆或正式配音。

## 安装包

- 文件：`release/袋研官矩阵混剪工作台-0.4.1-x64.exe`
- 大小：278,936,929 字节
- SHA256：`5C6E3EFA1890F735E3D9190E15B8225466717264A5A20D1E5BDF89D6C04714B1`
